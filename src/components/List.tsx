import React from 'react';

interface IProps {
    food: {
      title: string
      url: string
      calories: number
      description?: string
    }[]
  }

function List(props: IProps) {
    return (
        <div>
            
        </div>
    )
}

export default List
